# Grafana dashboards for my homelab

These are dashboards that I use in my homelab. They are in the `dashboards` directory.

### Test grafana

You can test grafana with a container using the `docker-compose.yml`

```sh
docker compose up -d
```
